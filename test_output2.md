## Section 1

At each node the calculation is

$$
a = \phi\!\left(\sum_{i} w_i x_i + b\right)
$$

`mermaid
flowchart LR
    W[Weighted Sum (z = \Sigma w\cdot x + b)]
    A[Activation (a = \phi(\phi (z)))]
    O[Training Time and Compute]
`
*Caption: test diagram*

---

## Section 2

Another section with math:

$$
E = mc^2
$$